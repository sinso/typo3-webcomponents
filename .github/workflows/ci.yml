name: CI

on: [push]

jobs:
  phpstan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: php-actions/composer@v6
        with:
          args: --ignore-platform-reqs

      - name: PHPStan Static Analysis
        run: vendor/bin/phpstan analyse -l 5 Classes/